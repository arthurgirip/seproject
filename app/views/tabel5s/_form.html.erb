<%= form_with(model: tabel5, local: true) do |form| %>
  <% if tabel5.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tabel5.errors.count, "error") %> prohibited this tabel5 from being saved:</h2>

      <ul>
      <% tabel5.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nume %>
    <%= form.text_field :nume, id: :tabel5_nume %>
  </div>

  <div class="field">
    <%= form.label :numar %>
    <%= form.number_field :numar, id: :tabel5_numar %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
